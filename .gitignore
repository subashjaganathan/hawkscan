# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# HawkScan test samples (contain EICAR / crafted binaries — would trip AV
# and GitHub malware scanning if committed). Regenerate locally as needed.
samples/

# Downloaded community rulesets live in the per-user cache, never in-repo.
hawkscan/rules/yara-forge-*
hawkscan/rules/yara-rules-*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
